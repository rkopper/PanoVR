using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NetworkModel : MonoBehaviour {
    // This should be network manager, or something.
    // Store / manage user id's / avatars / permissions?
}
